require 'spec_helper'

describe Play do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Play.create!(@valid_attributes)
  end
end
